spring:
    application:
      name: eurekaserver
    security:
      basic:
        enabled: true
      user:
        name: eureka-user
        password: admin123admin

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

